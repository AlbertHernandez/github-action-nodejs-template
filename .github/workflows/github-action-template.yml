---
name: 'github-action-template'

on:
  push:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout 🛬'
        uses: actions/checkout@v4
      - name: 'Github action template 🤩'
        uses: ./
